<?xml version="1.0"?>
<Container version="2">
  <Name>proxyseerr</Name>
  <Repository>ghcr.io/ronfburgundy/proxyseerr:latest</Repository>
  <Registry>https://github.com/RonFBurgundy/proxyseerr/pkgs/container/proxyseerr</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/RonFBurgundy/proxyseerr/issues</Support>
  <Project>https://github.com/RonFBurgundy/proxyseerr</Project>
  <Overview>
    A lightweight API routing proxy that lets Seerr drive two Sonarr
    instances and two Radarr instances through a single server connection each - typically a
    standard English library and a dedicated Anime library set up per the TRaSH Guides.

    Seerr can override a request's root folder and quality profile, but it cannot switch which
    instance a request is sent to. proxyseerr sits in between: it presents one merged API per
    service (both libraries, both root folders, both quality profiles), and routes each add,
    lookup, search command and queue call to the instance that actually owns it. Anime-side IDs
    are offset so the two instances never collide in Seerr's database.

    Point Seerr's Sonarr server at port 5000 and its Radarr server at port 5001, pick the anime
    root folder or the [Anime] quality profile in a Seerr override rule, and the request lands on
    the anime instance. Configure only the Sonarr pair or only the Radarr pair if you only need
    one of them.

    The proxy carries admin API keys for your servers, so it requires an API key of its own -
    generate one with `openssl rand -hex 24` and enter the same value in Seerr.

    Built and verified against Seerr and Sonarr/Radarr v4. Overseerr and Jellyseerr are untested.
  </Overview>
  <Category>MediaApp:Video MediaServer:Video Tools:Utilities</Category>
  <WebUI>http://[IP]:[PORT:5000]/proxy/health</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/RonFBurgundy/proxyseerr/main/unraid/proxyseerr.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/RonFBurgundy/proxyseerr/main/unraid/proxyseerr.png</Icon>
  <ExtraParams/>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Requires/>
  <License>MIT</License>
  <Config Name="Sonarr Proxy Port" Target="5000" Default="5000" Mode="tcp" Description="Port Seerr connects to for TV. Use this as the Sonarr port inside Seerr." Type="Port" Display="always" Required="true" Mask="false">5000</Config>
  <Config Name="Radarr Proxy Port" Target="5001" Default="5001" Mode="tcp" Description="Port Seerr connects to for movies. Use this as the Radarr port inside Seerr." Type="Port" Display="always" Required="false" Mask="false">5001</Config>
  <Config Name="Proxy API Key" Target="PROXY_API_KEY" Default="" Mode="" Description="Required. Generate with `openssl rand -hex 24` and enter the same value as the API key for both servers in Seerr. The proxy holds your Sonarr/Radarr admin keys, so it will not start unauthenticated unless PROXY_ALLOW_ANONYMOUS is set." Type="Variable" Display="always" Required="true" Mask="true"></Config>
  <Config Name="English Sonarr URL" Target="ENGLISH_SONARR_URL" Default="" Mode="" Description="Base URL of your standard TV Sonarr instance, using your server's LAN IP and Sonarr's port (default 8989) - the same address you use in a browser. Do NOT use localhost: on bridge networking that points at this container, not your server. This is the default instance: anything not matched as anime goes here. Leave all four Sonarr fields blank to run Radarr only." Type="Variable" Display="always" Required="false" Mask="false"></Config>
  <Config Name="English Sonarr API Key" Target="ENGLISH_SONARR_API_KEY" Default="" Mode="" Description="API key from the standard TV Sonarr (Settings - General - Security)." Type="Variable" Display="always" Required="false" Mask="true"></Config>
  <Config Name="Anime Sonarr URL" Target="ANIME_SONARR_URL" Default="" Mode="" Description="Base URL of your dedicated Anime Sonarr instance, using your server's LAN IP and that instance's port (commonly 8987 for a second Sonarr). Do NOT use localhost." Type="Variable" Display="always" Required="false" Mask="false"></Config>
  <Config Name="Anime Sonarr API Key" Target="ANIME_SONARR_API_KEY" Default="" Mode="" Description="API key from the Anime Sonarr instance." Type="Variable" Display="always" Required="false" Mask="true"></Config>
  <Config Name="English Radarr URL" Target="ENGLISH_RADARR_URL" Default="" Mode="" Description="Base URL of your standard movie Radarr instance, using your server's LAN IP and Radarr's port (default 7878). Do NOT use localhost. Leave all four Radarr fields blank to run Sonarr only." Type="Variable" Display="always" Required="false" Mask="false"></Config>
  <Config Name="English Radarr API Key" Target="ENGLISH_RADARR_API_KEY" Default="" Mode="" Description="API key from the standard movie Radarr instance." Type="Variable" Display="always" Required="false" Mask="true"></Config>
  <Config Name="Anime Radarr URL" Target="ANIME_RADARR_URL" Default="" Mode="" Description="Base URL of your dedicated anime movie Radarr instance, using your server's LAN IP and that instance's port (commonly 7879 for a second Radarr). Do NOT use localhost." Type="Variable" Display="always" Required="false" Mask="false"></Config>
  <Config Name="Anime Radarr API Key" Target="ANIME_RADARR_API_KEY" Default="" Mode="" Description="API key from the anime movie Radarr instance." Type="Variable" Display="always" Required="false" Mask="true"></Config>
  <Config Name="Allow Anonymous" Target="PROXY_ALLOW_ANONYMOUS" Default="false" Mode="" Description="Set to true only if you deliberately want the proxy to accept any caller on your LAN without an API key. Leaving this false and setting a Proxy API Key is strongly preferred." Type="Variable" Display="advanced" Required="false" Mask="false">false</Config>
  <Config Name="Anime Root Folder Match" Target="ANIME_ROOT_FOLDER_MATCH" Default="anime" Mode="" Description="Fallback keyword. If a request's root folder path contains this string and the path is not owned by a known instance, it is routed to the anime instance. Set it to an empty value to disable the fallback and route only on root folder ownership and profile IDs." Type="Variable" Display="advanced" Required="false">anime</Config>
  <Config Name="Anime Label Prefix" Target="ANIME_LABEL_PREFIX" Default="[Anime] " Mode="" Description="Cosmetic only: prefixes the anime instance's quality and language profile NAMES in Seerr's dropdowns so you can tell the two instances apart. It is never sent to Sonarr or Radarr, never becomes a tag, and never affects naming or downloads. Set it to an empty value to turn it off - useful if your anime profiles already have distinct names." Type="Variable" Display="advanced" Required="false">[Anime] </Config>
  <Config Name="Anime ID Offset" Target="ANIME_ID_OFFSET" Default="1000000000" Mode="" Description="Amount anime-side IDs are shifted by so the two instances share one ID space. Only change this before adding anything through the proxy - changing it later invalidates the IDs already stored in Seerr." Type="Variable" Display="advanced" Required="false">1000000000</Config>
  <Config Name="Upstream Timeout" Target="UPSTREAM_TIMEOUT" Default="20" Mode="" Description="Seconds to wait for an instance to respond. On an array with spun-down disks or a very large library, raise this to 45 - the log warns you when calls start approaching the limit." Type="Variable" Display="advanced" Required="false">20</Config>
  <Config Name="Upstream Connect Timeout" Target="UPSTREAM_CONNECT_TIMEOUT" Default="5" Mode="" Description="Seconds to wait to establish a connection. Kept short so a wrong URL fails fast; capped at the Upstream Timeout." Type="Variable" Display="advanced" Required="false">5</Config>
  <Config Name="Request Log" Target="REQUEST_LOG" Default="errors" Mode="" Description="errors = log every failed request (default), all = log every request including successful ones (useful while setting up), off = no access log. Routing decisions and every degraded path are logged regardless." Type="Variable" Display="advanced" Required="false">errors</Config>
  <Config Name="Log Level" Target="LOG_LEVEL" Default="INFO" Mode="" Description="DEBUG, INFO, WARNING or ERROR. INFO logs one line per routing decision." Type="Variable" Display="advanced" Required="false">INFO</Config>
</Container>
